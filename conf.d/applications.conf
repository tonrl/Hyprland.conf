#=================#
#   MY PROGRAMS   #
#=================#

# $terminal = kitty
# $terminal = uwsm app -- kitty
# $terminal = footclient.desktop
$terminal = foot
# $terminal = uwsm app -- alacritty
# $terminal = alacritty
$fileManager = footclient -e yazi
# $fileManager = kitty -e ranger
# $menu = rofi -show drun 
# $menu = app_menu.sh
$menu = rofi -show drun -run-command "uwsm app -- {cmd}"
# $menu = hyprlauncher
$ac_window = rofi -show window
$firefox = firefox.desktop
# $chrome = chromium
$firefox_beta = firefox-beta-bin.desktop
$chrome = chromium.desktop


